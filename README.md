# infantnetwork

Python package for constructing networks from infant transfer records. Functions are provided to construct networks and describe their shape and structure.

``` 
from infantnetwork import compute_network
compute_network(df)
```
